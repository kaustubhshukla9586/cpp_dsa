#include <iostream>
using namespace std; // this uses std before all the functions like we don't have to write std::cout only cout will work.

int main() {

    // std is a lib and cout is a function with std::cout we call the function.
    std::cout << "yo wwsup gng we diving deep in cpp\n"; // \n shifts to next line
    std::cout << "yo wwsup gng we diving deep in chpp" << std::endl;
    std::cout << "my nga" << endl;

    cout << "my nga is pro";

    return 0;
}   